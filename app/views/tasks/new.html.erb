<h1>タスクの新規作成</h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :content, 'タスク' %>
    <%= f.text_field :content %>
    
    <f.submit '作成' %>
<% end %>

<%= link_to 'タスク一覧に戻る', tasks_path %>